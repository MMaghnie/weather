[project]
name = "weather"
version = "0.1.0"
description = "Demo MCP Server Setup"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=2.1.1",
]

[project.scripts]
weather = "weather:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
